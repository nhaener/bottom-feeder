# brave (baseline)

Use `web_search` for quick source discovery.

Defaults:
- count: 5
- country: US (or topic-relevant)
- search_lang: en

Collect:
- title
- url
- key claim/snippet
- publication date when available

Use Brave in every routine run unless user disables web lookup.
